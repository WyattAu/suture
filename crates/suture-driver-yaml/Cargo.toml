[package]
name = "suture-driver-yaml"
version = "0.4.0"
edition = "2024"

[dependencies]
suture-driver = { path = "../suture-driver" }
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
