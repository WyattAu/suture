apiVersion: v1
kind: Service
metadata:
  name: {{ include "suture-hub.fullname" . }}
  labels:
    {{- include "suture-hub.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "suture-hub.selectorLabels" . | nindent 4 }}
